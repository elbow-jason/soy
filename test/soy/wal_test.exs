defmodule Soy.WalTest do
  use ExUnit.Case
  # import Soy.TestHelpers

  # doctest Soy.Iter

  # setup do
  #   {:ok, db, [_]} = Soy.open(tmp_dir())
  #   :ok = Soy.put(db, "a", "1")
  #   :ok = Soy.put(db, "k1", "v1")
  #   :ok = Soy.put(db, "k3", "v3")
  #   :ok = Soy.put(db, "k2", "v2")
  #   :ok = Soy.put(db, "z", "1000")
  # end
end
